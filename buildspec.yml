version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.21
  pre_build:
    commands:
      - echo Build started
      - go mod download github.com/aws/aws-lambda-go
      - go get github.com/TheBellman/photo-thumbnail
      - go test
  build:
    commands:
      - go mod download github.com/aws/aws-lambda-go
      - go get github.com/TheBellman/photo-thumbnail
      - go build
  post_build:
    commands:
      - echo Build completed
artifacts:
  files:
    - photo-thumbnail
  name: photo-thumbnail-$(date +%Y%m%d.%H%M%s).zip
